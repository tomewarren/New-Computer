# File Renamer
	cinst ant-renamer -y

# Development
	cinst sourcetree -y
	cinst cmder -y
	cinst github -y # Caused problems once. Remove if this happens again

# Digital entertainment
	cinst calibre -y
	cinst handbrake.install -y
	choco install mediatab -y # Media info in properties
	cinst mpc-hc -y
	cinst QuickPar -y

# Utilities
	cinst sandboxie -y
	cinst tightvnc -y
	cinst you-need-a-budget -y

	# Apply tweaks - PATH variables
	if(!(Test-Path -Path "C:\tools\" )){
		New-Item -ItemType directory -Path "c:\tools\"
	}
	Invoke-WebRequest 'https://raw.githubusercontent.com/iRobie/New-Computer/master/src/Personal-WHSFront-Tweaks.ps1' -OutFile 'C:\tools\Personal-WHSFront-Tweaks.ps1'
	&  'C:\tools\Personal-WHSFront-Tweaks.ps1'

	if (Test-PendingReboot) { Invoke-Reboot }
