
# Disable System Restore
    disable-computerrestore -drive "C:\"

# Disable hibernate
    powershell -Command "Start-Process 'powercfg.exe' -Verb runAs -ArgumentList '/h off'"


# File Renamer
	cinst ant-renamer -y

# Development
	cinst sourcetree -y
	# cinst github -y # Caused problems with continuing installation

# Digital entertainment
	cinst calibre -y
	cinst handbrake.install -y
	choco install mediatab -y # Media info in properties
	cinst mpc-hc -y
	cinst QuickPar -y
	cinst mkvtoolnix -y

# Utilities
	cinst sandboxie -y
	cinst tightvnc -y
	cinst btsync -y

	# Apply tweaks - PATH variables
	if(!(Test-Path -Path "C:\tools\" )){
		New-Item -ItemType directory -Path "c:\tools\"
	}
	Invoke-WebRequest 'https://raw.githubusercontent.com/iRobie/New-Computer/master/src/tweaks/Personal-WHSFront-Tweaks.ps1' -OutFile 'C:\tools\Personal-WHSFront-Tweaks.ps1'
	&  'C:\tools\Personal-WHSFront-Tweaks.ps1'
